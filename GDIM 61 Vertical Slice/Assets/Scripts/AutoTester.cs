using UnityEngine;

// The original click-to-take-order test flow no longer matches the game's
// rules (machine-color matching, no order-taking step). Kept as an empty
// component so GameBootstrap's AddComponent<AutoTester>() still resolves.
public class AutoTester : MonoBehaviour
{
}
